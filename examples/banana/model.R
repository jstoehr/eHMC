# -------------------- 
# --- BANANA MODEL
# --------------------

get_data <- function(data_dir = NULL) {
  return(list(sigma1 = 10, lambda = 0.03, lag = 100, sigma2 = 1))
}

# ------------------------------------------------------
# ------------------------------------------------------
# ------------------------------------------------------

get_pars_name <- function(data = get_data()) {
  return(c("theta1", "theta2"))
}