% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ehmc.R
\name{ehmc}
\alias{ehmc}
\title{Self-calibrated randomized Hamiltonian Monte Carlo}
\usage{
ehmc(
  iter,
  log_pdf,
  grad_log_pdf,
  warmup_samples,
  sampling_weights = NULL,
  chains = 1,
  init = NULL,
  control = list(),
  tuned_pars = NULL
)
}
\arguments{
\item{iter}{Integer. Number of sampling iterations per chain.}

\item{log_pdf}{Function. Log-density of the target distribution.}

\item{grad_log_pdf}{Function. Gradient of \code{log_pdf}.}

\item{warmup_samples}{Matrix. Warmup samples used for calibration. By
default, samples are stored by row.}

\item{sampling_weights}{Numeric vector. Importance weights associated with
\code{warmup_samples}. If \code{NULL}, uniform weights are used.}

\item{chains}{Integer. Number of chains to run. Default is 1.}

\item{init}{Numeric vector. Initial value for the chains. If \code{NULL}, each
chain is initialized by sampling from \code{warmup_samples} according to
\code{sampling_weights}.}

\item{control}{List of control parameters:
\describe{
\item{\code{by_row}}{Logical. Whether \code{warmup_samples} stores samples by row.
Default is \code{TRUE}.}
\item{\code{metric}}{Character. Mass matrix type: \code{"diag"}, \code{"dense"}, or
\code{"unit"}. Default is \code{"diag"}.}
\item{\code{inv_M}}{Matrix. Initial inverse mass matrix. Default is the identity
matrix. Ignored when \code{metric = "unit"}.}
\item{\code{adapt_M}}{Logical. Whether to adapt the mass matrix. Default is
\code{TRUE}.}
\item{\code{reg_param}}{Numeric. Regularization parameter used during mass
matrix adaptation. Default is \code{1e-1}.}
\item{\code{stepsize}}{Numeric. Initial leapfrog step size. Default is \code{1e-1}.}
\item{\code{adapt_stepsize}}{Logical. Whether to adapt the step size. Default is
\code{TRUE}.}
\item{\code{epoch_adapt}}{Integer. Number of adaptation epochs. Default is 1.}
\item{\code{max_length}}{Numeric. Maximum number of leapfrog steps considered
during adaptation. Default is \code{2e16}.}
\item{\code{adapt_delta}}{Numeric. Target acceptance probability for step-size
adaptation. Default is \code{0.651}. Ignored when \code{adapt_stepsize = FALSE}.}
\item{\code{refresh_rate}}{Numeric. Probability of refreshing the momentum at
each iteration. Default is 1.}
\item{\code{rand_leapfrog}}{Logical. Whether to randomize the number of
leapfrog steps when momentum is not refreshed. Default is \code{TRUE}.}
\item{\code{min_eps}}{Numeric. Minimum step size allowed during adaptation.
Default is \code{1e-12}.}
\item{\code{tol}}{Numeric. Tolerance used when targeting \code{adapt_delta}.
Default is \code{0.05}.}
\item{\code{max_bisect_iter}}{Integer. Maximum number of bisection iterations
used during step-size adaptation. Default is 50.}
}}

\item{tuned_pars}{Optional list of precomputed tuning parameters. If supplied,
calibration is skipped. Expected entries are \code{weights__},
\code{n_leapfrogs__}, \code{stepsizes__}, and \code{inv_M__}.}
}
\value{
A list with components:
\describe{
\item{\code{sim}}{Simulation output:
\describe{
\item{\code{samples}}{List of matrices, one per chain, containing posterior
draws.}
\item{\code{lp__}}{List of log-density values, one vector per chain.}
\item{\code{freq_accept}}{Numeric vector of acceptance frequencies, one per
chain.}
\item{\code{diagnosis}}{List of data frames, one per chain, containing
sampler diagnostics:
\describe{
\item{\code{accept_stat__}}{Metropolis acceptance probability for each
transition.}
\item{\code{stepsize__}}{Leapfrog integrator step size used at the
iteration.}
\item{\code{n_leapfrog__}}{Number of leapfrog steps performed at the
iteration. With partial momentum refreshment, this may be smaller than the
number of leapfrog steps drawn from the empirical distribution,
because previously computed trajectory points can be reused.}
\item{\code{index__}}{Index of the atom of the discrete empirical
distribution used to sample the step size and the number of
leapfrog steps.}
\item{\code{move__}}{Logical indicator of whether a transition
is accepted}
\item{\code{divergent__}}{Logical indicator of whether a divergent
transition occurred.}
\item{\code{delta_energy_stat__}}{Change in Hamiltonian energy across
the trajectory.}
\item{\code{energy__}}{Hamiltonian energy of the proposed state.}
}}
}}
\item{\code{tuned_pars}}{List of tuning parameters used for sampling.}
\item{\code{adaptation_settings}}{Resolved control settings used for the run.}
}
}
\description{
Runs Hamiltonian Monte Carlo with randomized integration times calibrated
from weighted warmup samples.
}
\examples{
\dontrun{
# Example to be added.
}

}
